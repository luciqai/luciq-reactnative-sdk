<manifest 
  xmlns:android="http://schemas.android.com/apk/res/android">

  <application>
    <provider
      android:name="ai.luciq.reactlibrary.RNLuciqStartupProvider"
      android:authorities="${applicationId}.luciq.startup"
      android:exported="false"
      android:initOrder="1" />
  </application>

</manifest>
